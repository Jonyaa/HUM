const join_window = document.getElementsByClassName("form_wrap")[0];

function join_room() {
    // Open and close the join room form window
    join_window.classList.toggle("show");
}

function create_room() {
    console.log("Create Room")
}